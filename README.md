# snowmy-tech AWS plugins

AWS topic marketplace. Each plugin represents a complete AWS service or coherent service domain; language SDKs remain separate.

```bash
codex plugin marketplace add snowmy-tech/aws-plugins --ref main
```
